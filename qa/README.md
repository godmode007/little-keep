# First-level playtests

Phone-viewport recordings of the playable slice (Expo web, 390×844).

| Video | Beats |
|-------|--------|
| `videos/01-first-level.mp4` | Title → collect → Pip → deeper woods locked → clear near woods → Help the Baker → +2 stars → Garden Trolls is the next hook |
| `videos/02-fog-chain.mp4` | Deeper woods refuse skip → tower 1 → collect → tower 2 |

Re-record (Expo must be on `http://localhost:8081`):

```bash
cd qa && npm install && node record-first-level.mjs
```
